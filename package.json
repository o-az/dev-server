{
  "type": "module",
  "scripts": {
    "dev": "node --import=tsx --no-warnings runner.ts",
    "build": "node --import=tsx --no-warnings esbuild.config.ts",
    "build:watch": "node --import=tsx --no-warnings esbuild.config.ts --watch"
  },
  "dependencies": {
    "esbuild": "^0.19.5",
    "glob": "^10.3.10",
    "tsx": "^3.14.0",
    "vite": "^4.4.11",
    "vite-node": "^0.34.6"
  },
  "devDependencies": {
    "@types/node": "^20.8.6",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=20"
  }
}
